doctype html
html
  include ../structure/head.pug
  body
    include ../structure/header.pug
    .columns.is-mobile
      .column
      .column
        if method == 'PATCH'
          form(action='/services/'+service.id+'?_method=PATCH', method='post')
            label.label Titre du service
              .control
                input(name="title", cols="30", rows="10", type="text", value=service.title).input
            label.label Ville
              .control
                input(name="city", cols="30", rows="10", type="text", value=service.city).input
            label.label Message
              .control
                textarea(class="service" name="message", cols="30", rows="10").textarea.has-fixed-size #{service.message}
            label.label Terminer  
              .checkbox
                if service.completion 
                  input(type='checkbox', name='completion', checked)
                else
                  input(type='checkbox', name='completion')
            input(type='submit', value='Valider').button.is-success
        else
          form(action='/services', method='post')
            label.label Titre du service
              .control
                input(name="title", cols="30", rows="10", type="text").input
            label.label Ville
              .control
                input(name="city", cols="30", rows="10", type="text").input
            label.label Message
              .control
                textarea(class="service" name="message", cols="30", rows="10").textarea.has-fixed-size
            label.label Terminer  
              .checkbox
                input(type='checkbox', name='completion')
            input(type='submit', value='Valider').button.is-success
      .column
footer
    include ../structure/footer.pug